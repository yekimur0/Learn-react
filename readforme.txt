1. Хук useState - отвечает за состояние отоброжаемых элементов на странице
  /* для того чтобы изменить состояние какого - либо объекта мы используем хук useState => передаем записываем в переменную ввиде массива (const [value, namefunction] = useState(значение))

 - дальше описываем сами функции которые должны выполнить нужное дейсвтие 
 function name () { 

   Используем функцию в переменной
   nameFunction(передаем дейсвтие которое требуется)

 }
   